# OtaCopilotApp


## Comandos

Primero instala todas las dependencias con:

``` bash
npm install
```

Para correr la db de json-server:

``` bash
npm run db
```

Para correr la app:

``` bash
ng serve
```
